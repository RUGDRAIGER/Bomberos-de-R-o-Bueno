import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.bomberos.riobueno.pof',
  appName: 'POF Bomberos Rio Bueno',
  webDir: 'dist',
}

export default config
